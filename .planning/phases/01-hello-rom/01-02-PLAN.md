---
phase: 01-hello-rom
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/Emulator.zig
  - src/cpu.zig
  - src/bus.zig
  - src/cartridge/mod.zig
  - src/cartridge/rom_only.zig
autonomous: true
requirements:
  - CORE-01
  - CPU-01
  - CPU-03
  - CPU-04
  - CPU-05
  - BUS-01
  - BUS-02
  - BUS-04
  - CART-01
must_haves:
  truths:
    - "CPU register file is a packed struct with proper pair getter/setter methods"
    - "All 256 main opcodes + 256 CB-prefix opcodes are dispatched via comptime tables"
    - "Bus dispatches reads/writes to the correct memory region by address"
    - "Open-bus reads (unmapped addresses) return 0xFF"
    - "Echo RAM at 0xE000-0xFDFF mirrors WRAM at 0xC000-0xDDFF"
    - "Every bus read/write advances cycle count and ticks peripheral stubs"
    - "ROM-only cartridge loads ROM bytes and parses header (title, type, size, checksum)"
    - "HALT-bug: HALT with IME=0 and pending IRQ exits HALT without dispatch, PC stays"
    - "Undocumented opcodes execute as NOP + advance PC"
  artifacts:
    - path: src/cpu.zig
      provides: "LR35902 registers, flag register, 256+256 comptime opcode tables, fetch/decode/execute"
      contains: "packed struct"
      min_lines: 400
    - path: src/bus.zig
      provides: "MMU read8/write8 dispatch, cycle accounting, MMIO packed struct, peripheral stubs"
      contains: "switch (addr >> 12)"
      min_lines: 250
    - path: src/cartridge/rom_only.zig
      provides: "ROM-only cart loader, header parser (title/type/size/checksum)"
      contains: "header_checksum"
      min_lines: 60
    - path: src/Emulator.zig
      provides: "Full Emulator struct integrating CPU + Bus + Cartridge"
      contains: "stepInstruction"
      min_lines: 60
  key_links:
    - from: src/cpu.zig
      to: src/bus.zig
      via: "CPU calls bus.read8/bus.write8 for all memory access"
      pattern: "self\\.bus\\.(read8|write8)"
    - from: src/bus.zig
      to: src/cartridge/rom_only.zig
      via: "Bus dispatches 0x0000-0x7FFF reads to cart.read_rom()"
      pattern: "cart\\.read_rom"
    - from: src/Emulator.zig
      to: src/cpu.zig, src/bus.zig, src/cartridge/mod.zig
      via: "Emulator struct owns Cpu, Bus, Cart fields"
      pattern: "(cpu|cpu|bus|cart):"
---

<objective>
Implement the Sharp LR35902 CPU core with packed-struct register file and comptime opcode dispatch (256 main + 256 CB-prefix), the bus-centered MMU with full 16-bit address dispatch and cycle accounting, and the ROM-only cartridge loader with header parsing. Integrate all three into the Emulator struct.

**Purpose**: This is the emulator's engine. After this plan, the CPU can decode and execute every LR35902 instruction, the bus dispatches every memory access with correct timing, and ROM-only cartridges load with validated headers. All emulator-core source files exist in their final form.

**Output**: A complete but untested emulator core (CPU + Bus + Cartridge) in `src/`. No test runner yet — that's Plan 01-03.
</objective>

<execution_context>
@/home/ksante/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/ksante/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-hello-rom/01-CONTEXT.md
@.planning/phases/01-hello-rom/01-RESEARCH.md
@.planning/research/ARCHITECTURE.md
@.planning/research/STACK.md
@.planning/phases/01-hello-rom/01-01-PLAN.md
</context>

## Phase Goal

**As a** developer, **I want to** build the complete CPU, bus, and cartridge core, **so that** any ROM-only Game Boy game can be loaded and its instructions executed with cycle-accurate timing.

---

## Artifacts This Plan Produces

| Artifact | Purpose | Consumers |
|----------|---------|-----------|
| `src/cpu.zig` | LR35902 register file (packed struct), comptime opcode dispatch tables, fetch/decode/execute, HALT-bug, M-cycle timing, undocumented opcode handling | Plan 01-03 (test runner), Plan 02 (timer/interrupts), all future plans |
| `src/bus.zig` | Bus read8/write8 switch dispatch, MMIO packed struct with comptime asserts, WWW (8 KiB) + HRAM (127 B) + VRAM stub (8 KiB) + OAM stub (160 B), echo RAM mirror, open-bus 0xFF, serial output capture stub, timer/PPU/joypad stubs, tick(n) cycle fanout | Plan 01-03 (serial capture), Plan 02 (timer activation), Plan 03 (PPU) |
| `src/cartridge/mod.zig` | Re-export of rom_only.zig | Plan 02 (MBC1/2/3/5 dispatch) |
| `src/cartridge/rom_only.zig` | ROM-only cart loader, header parser (title at 0x0134, type at 0x0147, ROM size at 0x0148, RAM size at 0x0149, checksum at 0x014D), read_rom(addr) | Plan 02 (MBC1 activation) |
| `src/Emulator.zig` (updated) | Full struct: init(allocator, rom_bytes), stepInstruction(), stepMCycle(), runForFrames(n), getSerialOutput(), getFrameBuffer(), deinit() | Plan 01-03 (test runner), Plan 03 (SDL3 host) |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement CPU register file and comptime opcode dispatch</name>
  <files>
    src/cpu.zig
  </files>
  <read_first>
    @.planning/phases/01-hello-rom/01-CONTEXT.md (D-17..D-22)
    @.planning/phases/01-hello-rom/01-RESEARCH.md (lines 192-229 for comptime dispatch pattern, lines 323-338 for register file pattern, lines 394-418 for decode table, lines 469-489 for M-cycle table)
    @.planning/research/ARCHITECTURE.md (lines 291-375 for packed struct register file and MMIO offset assertions)
  </read_first>
  <behavior>
    - Test 1: Register file packed struct layout: 8*u8 + sp:u16 + pc:u16 = 12 bytes. @sizeOf(Registers) == 12. getBc() returns (b << 8) | c. setBc(v) sets b = v>>8, c = v&0xFF. setAf(v) masks f with 0xF0.
    - Test 2: Comptime main_decode[256] and cb_decode[256] tables: every entry has valid mnemonic, length (1-3), mcycles_taken. No .invalid entries in main table (all 256 have valid decode).
    - Test 3: fetch_decode advances pc by instruction length. For opcode 0x00 (NOP): pc += 1. For 0x01 (LD BC, imm16): pc += 3.
    - Test 4: HALT-bug: when HALT executed and IME=0 and (IE & IF) != 0, CPU exits HALT but pc stays at HALT address (does NOT advance). Next instruction re-executes the byte after HALT.
    - Test 5: Undocumented opcodes (0xD3, 0xDB, 0xDD, 0xE3, 0xE4, 0xEB, 0xEC, 0xED, 0xF4, 0xFC, 0xFD) execute as NOP: pc advances by 1, no register changes, no bus access beyond fetch.
    - Test 6: Flag register: write to F masks with 0xF0. Read of F shows low 4 bits always 0.
  </behavior>
  <action>
    Create `src/cpu.zig` with the following structure:

    **Registers** (D-17, D-18):
    - `FlagRegister` packed struct: `_unused: u4` (always 0), `carry: u1`, `half_carry: u1`, `subtract: u1`, `zero: u1`. Add `@sizeOf(FlagRegister) == 1` comptime assert.
    - `Registers` packed struct: `a: u8, f: u8, b: u8, c: u8, d: u8, e: u8, h: u8, l: u8, sp: u16, pc: u16`. Add `@sizeOf(Registers) == 12` comptime assert.
    - Getter/setter methods: `getAf()`, `setAf(v)`, `getBc()`, `setBc(v)`, `getDe()`, `setDe(v)`, `getHl()`, `setHl(v)` — all using `(hi << 8) | lo` pattern. setAf masks f with `& 0xF0`. setF writes through `@bitCast(FlagRegister)` then masks.
    - `getFlags()` returns `@bitCast(FlagRegister)` from `self.f`. `setFlags(flags)` sets `self.f = @as(u8, @bitCast(flags)) & 0xF0`.

    **Instruction Decode** (D-19, D-20):
    - Define `Instruction` tagged union with ~30 variants covering all opcode groups: `nop`, `ld_r16_imm16`, `ld_r8_imm8`, `ld_r8_r8`, `ld_r16_a`, `ld_a_r16`, `ld_a_c`, `ld_c_a`, `ld_a_imm16`, `ld_imm16_a`, `ld_hl_plus_a`, `ld_hl_minus_a`, `inc_r16`, `dec_r16`, `inc_r8`, `dec_r8`, `add_a_r8`, `adc_a_r8`, `sub_a_r8`, `sbc_a_r8`, `and_a_r8`, `xor_a_r8`, `or_a_r8`, `cp_a_r8`, `jp_imm16`, `jp_hl`, `jr_rel`, `call_imm16`, `ret`, `ret_cond`, `rst_vec`, `push_r16`, `pop_r16`, `halt`, `stop`, `ei`, `di`, `cb_prefix`, `invalid`.
    - `OpcodeEntry` struct: `mnemonic: Instruction.Tag`, `length: u3`, `mcycles: u4`, `mcycles_not_taken: u4` (for conditionals). Also store operand info in comptime struct.
    - Comptime `main_table[256]`: populate via switch on opcode byte. Each entry specifies mnemonic tag, length, M-cycles (taken and not-taken for conditionals). For conditional opcodes like `JR NZ, r8` (0x20): length=2, mcycles=3 (taken), mcycles_not_taken=2.
    - Comptime `cb_table[256]`: same pattern for CB-prefixed operations (RL/RR/RLC/RRC/SLA/SRA/SWAP/BIT/RES/SET + undocumented).
    - `@setEvalBranchQuota(10000)` for both tables.
    - Undocumented opcodes per D-22: all opcodes not documented for LR35902 are in the main table as `.nop` variant with length=1, mcycles=1. In CB table, undocumented entries are also `.nop`.

    **CPU Struct**:
    - Fields: `regs: Registers`, `ime: bool` (interrupt master enable), `ime_next: bool` (deferred IME per D-21 EI-delay), `halted: bool`, `halt_bug: bool` (D-21), `bus: *Bus` (pointer to shared bus).
    - `init(bus: *Bus)` — initialize registers to DMG reset values (A=0x01, F=0xB0, B=0x00, C=0x13, D=0x00, E=0xD8, H=0x01, L=0x4D, SP=0xFFFE, PC=0x0100). IME=false, IME_next=false, halted=false, halt_bug=false.
    - `fetchDecode()` — read opcode via `bus.read8(regs.pc)`, handle halt_bug flag (if set, do NOT advance pc; clear halt_bug). Otherwise advance pc by 1. Return decoded instruction from main_table.
    - `execute(inst: Instruction)` — large switch on inst variant tag. For each variant:
      - Perform the operation on registers
      - For memory operations, call `bus.read8(...)` or `bus.write8(...)`
      - After each instruction, apply `ime = ime_next` (EI delay per D-21)
      - Check interrupts: if IME and any bit in (IE & IF) is set, enter interrupt handler
      - For HALT: set halted=true, if (IE & IF) != 0 and !IME: set halt_bug=true, clear halted
      - For HALT wake: if halted and any interrupt pending: clear halted
    - `stepInstruction()` — calls fetchDecode(), gets mcycles from main_table, calls execute(), calls bus.tick(mcycles). For CB prefix: fetch second byte, dispatch via cb_table, tick CB mcycles.
    - `stepMCycle()` — single M-cycle step (for fine-grained control per D-09).
    - For EI: set ime_next = true. For DI: set ime_next = false AND ime = false (immediate per D-21).
    - Conditional RET M-cycles (CPU-04): if condition not met, use mcycles_not_taken from table.

    **M-cycle tables** (D-23):
    - Separate comptime arrays: `main_mcycles[256]u4` and `cb_mcycles[256]u4`.
    - `getMainMCycles(opcode: u8, condition_met: ?bool) u4` — returns mcycles, using conditional variant if applicable.

    All CPU code uses zero allocations. Registers are inline in the struct. No ArrayList, no dynamic dispatch.
  </action>
  <verify>
    <automated>zig build 2>&1</automated>
  </verify>
  <done>
    cpu.zig compiles. Register file passes `@sizeOf` asserts. Comptime tables produce valid entries for all 512 opcodes. No .invalid or .unreachable entries in the main decode table.
  </done>
  <acceptance_criteria>
    - cpu.zig compiles without errors
    - Registers packed struct is exactly 12 bytes (@sizeOf(Registers) == 12)
    - FlagRegister is exactly 1 byte (@sizeOf(FlagRegister) == 1)
    - Comptime tables compile without `@setEvalBranchQuota` overflow
    - All 256 main opcodes have valid entries (no invalid/unreachable in main table)
    - ~30 undocumented opcodes in CB table have NOP entries
    - HALT-bug flag implemented per D-21
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement bus MMU with address dispatch, cycle accounting, and peripheral stubs</name>
  <files>
    src/bus.zig
  </files>
  <read_first>
    @.planning/phases/01-hello-rom/01-CONTEXT.md (D-13..D-16, D-23, D-24)
    @.planning/phases/01-hello-rom/01-RESEARCH.md (lines 231-251 for bus dispatch pattern, lines 340-387 for MMIO packed struct pattern)
    @.planning/research/ARCHITECTURE.md (lines 140-158 for memory map, lines 340-387 for packed struct MMIO)
  </read_first>
  <behavior>
    - Test 1: read8(0x0000-0x7FFF) dispatches to cart.read_rom(). Returns first ROM byte.
    - Test 2: read8(0xC000) returns WRAM byte; write8(0xC000, 0x42); read8(0xC000) returns 0x42.
    - Test 3: Echo RAM: write8(0xC000, 0x42); read8(0xE000) returns 0x42. Write Echo, read WRAM confirms mirror.
    - Test 4: Open-bus: read8(0xFEA0) returns 0xFF. read8(0xA000) returns 0xFF (no cart RAM).
    - Test 5: MMIO packed struct offset assertions: @offsetOf(MMIO, field) matches hardware address for all defined registers.
    - Test 6: tick(n) advances internal cycle counter by n. Multiple ticks accumulate.
  </behavior>
  <action>
    Create `src/bus.zig` with the following structure:

    **MMIO packed struct** (D-24):
    - Define packed struct for the 256-byte IO region (0xFF00-0xFFFF):
      - `JOYP: u8` at offset 0x00 (D-14: stub, initial value 0xCF)
      - `SB: u8` at offset 0x01 (serial data byte)
      - `SC: u8` at offset 0x02 (serial control)
      - `_pad_03: u8`
      - `DIV: u8` at offset 0x04 (D-15: static stub = 0x00)
      - `TIMA: u8` at offset 0x05 (static stub = 0x00)
      - `TMA: u8` at offset 0x06 (static stub = 0x00)
      - `TAC: u8` at offset 0x07 (static stub = 0x00)
      - `_pad_08_0E: [7]u8`
      - `IF: u8` at offset 0x0F (interrupt flag, D-14 stub)
      - `_pad_10_3F: [48]u8` (APU + wave RAM stubs, D-16)
      - `_pad_40_4F: [16]u8` (PPU IO region stub, D-16)
      - `BANK: u8` at offset 0x50 (boot ROM toggle)
      - `_pad_51_7E: [46]u8` (OAM DMA + misc)
      - `_pad_7F: u8`
      - `_pad_80_FE: [127]u8` (HRAM — handled separately)
      - `IE: u8` at offset 0xFF (interrupt enable)
    - Comptime block with assertions: `@offsetOf(MMIO, "JOYP") == 0x00`, `@offsetOf(MMIO, "SB") == 0x01`, `@offsetOf(MMIO, "DIV") == 0x04`, `@offsetOf(MMIO, "IF") == 0x0F`, `@offsetOf(MMIO, "IE") == 0xFF`, `@sizeOf(MMIO) == 256`.

    **Bus Struct**:
    - Fields: `wram: [8 * 1024]u8` (WRAM 0xC000-0xDFFF), `hram: [127]u8` (HRAM 0xFF80-0xFFFE), `vram_stub: [8 * 1024]u8` (VRAM stub, D-16, initialized to 0xFF), `oam_stub: [160]u8` (OAM stub, D-16, initialized to 0xFF), `mmio: MMIO`, `cart: *Cart`, `serial_output: [256]u8` (serial capture buffer), `serial_index: usize`, `t_cycles: u64` (accumulated T-cycles).
    - `init()` — set WRAM/HRAM/VRAM/OAM to 0xFF via `@memset`. Set MMIO fields to their reset values. Zero serial_output. Set t_cycles to 0.
    - `read8(addr: u16) u8`:
      - Increment `t_cycles += 4` (each M-cycle = 4 T-cycles).
      - Switch on `addr >> 12`:
        - `0x0-0x7` → `self.cart.read_rom(addr)` (ROM)
        - `0x8-0x9` → `self.vram_stub[addr - 0x8000]` (VRAM stub, D-16)
        - `0xA-0xB` → `0xFF` (cart RAM not present for ROM-only, open-bus, D-13)
        - `0xC-0xD` → `self.wram[addr - 0xC000]` (WRAM)
        - `0xE` → `self.wram[addr - 0xE000]` (Echo RAM, BUS-02: mirrors WRAM 0xC000-0xDDFF)
        - `0xF` → inner switch:
          - `0xFE00-0xFE9F` → `self.oam_stub[addr - 0xFE00]` (OAM stub, D-16)
          - `0xFEA0-0xFEFF` → `0xFF` (unusable area, open-bus, D-13)
          - `0xFF00-0xFF7F` → `self.mmio_read(@intCast(addr & 0xFF))`
          - `0xFF80-0xFFFE` → `self.hram[addr - 0xFF80]`
          - `0xFFFF` → `self.mmio.IE`
    - `write8(addr: u16, val: u8) void`:
      - Increment `t_cycles += 4`.
      - Same dispatch pattern as read8, with appropriate write handling.
      - For 0xE range (Echo RAM): write to `wram[addr - 0xE000]`.
      - For 0xFF00-0xFF7F: call `mmio_write(offset, val)`.
      - For 0xFF80-0xFFFE: write to `hram[addr - 0xFF80]`.
      - For 0xFFFF: write to `mmio.IE`.
      - ROM and unmapped regions: writes are silently ignored (DMG behavior).
    - `mmio_read(offset: u8) u8`:
      - Default: return `std.mem.asBytes(&self.mmio)[offset]`.
      - SB (0x01): return serial byte.
      - SC (0x02): return serial control byte.
      - DIV (0x04): return current DIV value (static 0x00 per D-15).
      - JOYP (0x00): return value & 0x0F (only low 4 bits valid, D-14).
    - `mmio_write(offset: u8, val: u8) void`:
      - SB (0x01): capture byte to serial_output buffer if serial_index < 256 (D-14). Set `serial_output[serial_index] = val; serial_index += 1;`.
      - SC (0x02): if val & 0x81 == 0x81 (transfer start + clock), this is Blargg's serial trigger. Capture the pending SB byte. Store val in mmio bytes.
      - Default: write to `std.mem.asBytes(&self.mmio)[offset]`.
    - `tick(n: u4) void` — advance `t_cycles += n * 4`. In Phase 1, this is a cycle counter accumulator. In Phase 2+, this fans out to timer/PPU.
    - `getSerialOutput() []const u8` — returns `serial_output[0..serial_index]`.
    - `getFrameBuffer() *const [160*144]u8` — returns stub (zeros in Phase 1).
  </action>
  <verify>
    <automated>zig build 2>&1</automated>
  </verify>
  <done>
    bus.zig compiles. MMIO packed struct passes all comptime offset assertions. read8/write8 dispatch covers all 16 address regions. Serial output capture stores bytes correctly.
  </done>
  <acceptance_criteria>
    - bus.zig compiles without errors
    - MMIO packed struct passes comptime @offsetOf assertions for all defined registers
    - Address dispatch switch covers all 16 possible nibble values (0x0-0xF)
    - Echo RAM at 0xE reads from wram with offset 0xE000
    - Unmapped reads (VRAM, OAM, unused areas) return 0xFF
    - Serial capture stores SB bytes in serial_output buffer
    - tick(n) accumulates t_cycles by n * 4
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Implement ROM-only cartridge loader and integrate CPU + Bus + Cartridge into Emulator</name>
  <files>
    src/cartridge/mod.zig
    src/cartridge/rom_only.zig
    src/Emulator.zig
  </files>
  <read_first>
    @.planning/phases/01-hello-rom/01-CONTEXT.md (D-25, D-27, D-09..D-12)
    @.planning/phases/01-hello-rom/01-RESEARCH.md (lines 55-57 for header checksum, lines 435-467 for serial capture pattern)
    @.planning/phases/01-hello-rom/01-RESEARCH.md (lines 424-453 for Emulator struct pattern)
  </read_first>
  <action>
    Create three files:

    **`src/cartridge/rom_only.zig`** (D-27, CORE-01, CART-01):
    - `RomOnly` struct:
      - Fields: `rom: []const u8` (owned slice of ROM bytes), `title: [16]u8`, `cart_type: u8`, `rom_size: u8`, `ram_size: u8`, `checksum: u8`, `checksum_ok: bool`.
    - `load(allocator: std.mem.Allocator, rom_bytes: []const u8) !RomOnly`:
      - Validate rom_bytes.len >= 0x8000 (minimum 32 KiB for a valid ROM; accept smaller for tests).
      - Copy title from bytes[0x0134..0x0143] (null-terminated or space-padded, 16 bytes).
      - Read cart_type from bytes[0x0147].
      - Read rom_size from bytes[0x0148].
      - Read ram_size from bytes[0x0149].
      - Read checksum from bytes[0x014D].
      - Compute checksum: sum of bytes[0x0134..0x014C] modulo 0x100. Compare with byte[0x014D].
      - If mismatch: `std.log.warn("header checksum mismatch: expected 0x{X:02}, got 0x{X:02}", .{ expected, actual })` per D-27. NEVER fail.
      - Store `checksum_ok` flag.
      - Allocate and copy rom bytes for owned storage (caller may free the original).
    - `readRom(self: *RomOnly, addr: u16) u8`:
      - Return `self.rom[addr]` for addr in 0x0000-0x7FFF range.
      - No banking for ROM-only (CART-01): addr maps directly into the ROM array.
      - `@intCast` is safe because addr is always in range for valie ROM.

    **`src/cartridge/mod.zig`**:
    - Re-export: `pub const RomOnly = @import("rom_only.zig").RomOnly;`

    **`src/Emulator.zig`** (replace stub from Plan 01-01):
    - Import: `@import("cpu.zig").Cpu`, `@import("bus.zig").Bus`, `@import("cartridge/mod.zig").RomOnly`.
    - Fields:
      - `cpu: Cpu`, `bus: Bus`, `cart: RomOnly`
      - `allocator: std.mem.Allocator`
      - `rom_slice: []const u8` (owned ROM bytes)
    - `init(allocator: std.mem.Allocator, rom_bytes: []const u8) !Emulator` per D-10:
      - Copy rom_bytes into `rom_slice` (owned allocation).
      - Initialize `cart` via `RomOnly.load(allocator, rom_slice)`.
      - Initialize `bus` via `Bus.init(&cart)`.
      - Initialize `cpu` via `Cpu.init(&bus)`.
      - Return Emulator with all fields populated.
    - `deinit(self: *Emulator) void`:
      - `allocator.free(self.rom_slice)`.
      - No other deinit needed (bus/cpu are inline, no heap in cart).
    - `stepInstruction(self: *Emulator) void` (D-09):
      - Call `self.cpu.stepInstruction()`.
    - `stepMCycle(self: *Emulator) void` (D-09):
      - Call `self.cpu.stepMCycle()`.
    - `runForFrames(self: *Emulator, frames: u32) void` (D-09):
      - For each frame: advance CPU until PPU would have reached VBlank (stub: advance a fixed number of instructions ~17556 per frame). In Phase 1, this is a loop that calls stepInstruction ~17556 times per frame. (17556 ≈ 70224 dots / 4 dots-per-M-cycle = ~17556 M-cycles per frame.)
    - `getSerialOutput(self: *Emulator) []const u8` per D-14:
      - Return `self.bus.getSerialOutput()`.
    - `getFrameBuffer(self: *Emulator) *const [160*144]u8` per D-12:
      - Return stub zeroed buffer (Phase 1: no rendering). Static buffer on Emulator: `framebuffer: [160*144]u8`.
    - Error handling per D-11: methods return `!void`, using error union for propagation. No panic.
  </action>
  <verify>
    <automated>zig build 2>&1</automated>
  </verify>
  <done>
    Full emulator compiles. Emulator.init creates CPU + Bus + Cart connected by pointers. stepInstruction() advances CPU state through at least one instruction without crashing.
  </done>
  <acceptance_criteria>
    - `zig build` compiles without errors
    - Cartridge header parse extracts title (up to 16 chars from 0x0134)
    - Checksum validation emits warning on mismatch, never errors
    - Emulator.init creates connected CPU/Bus/Cart with correct pointers
    - Emulator struct has all 3 stepping methods, getSerialOutput(), getFrameBuffer()
    - No .panic or .unreachable in hot path (fetch/decode/execute)
    - All memory state explicitly initialized (@memset), no undefined bytes
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ROM bytes → cartridge header parser | Untrusted binary data parsed as header fields |
| ROM bytes → bus ROM dispatch | Untrusted binary data read as code by CPU |
| Bus → serial_output buffer | Bounded write (256 bytes), no overflow possible |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Tampering | Cartridge header parser at 0x0134-0x014D | mitigate | Byte range bounds-checked: only access valid ROM offsets. Accept minimum 0x8000 bytes. |
| T-01-04 | Tampering | CPU fetch/decode at any ROM address | accept | ROM is loaded in full; reads are bounds-checked via `@intCast` only. For out-of-bounds reads, Zig debug mode catches via safety checks. ReleaseFast relies on ROM size validation at init. |
| T-01-05 | Denial of Service | Oversized ROM allocation | mitigate | Cap ROM allocation to 8 MiB (max DMG cart) via load-time size check. Return error if exceeded. |
| T-01-06 | Information Disclosure | Undefined memory state in ReleaseFast | mitigate | All state arrays explicitly zero-initialized via `@memset` in Bus.init. Never rely on undefined zeroing. |
</threat_model>

<verification>
1. `zig build` produces no errors or warnings
2. All comptime assertions pass (register size, MMIO offsets)
3. Emulator.init creates valid CPU/Bus/Cart state
4. Emulator.stepInstruction() runs at least one instruction without panic
5. All memory is explicitly initialized (no undefined state)
</verification>

<success_criteria>
- [ ] All 3 tasks complete
- [ ] cpu.zig contains packed struct register file + comptime 256+256 opcode tables + HALT-bug + undocumented opcodes
- [ ] bus.zig contains full 16-bit address dispatch + cycle accounting + MMIO packed struct with comptime asserts + serial stub
- [ ] cartridge/rom_only.zig loads ROM, parses header (title/type/size/checksum), validates checksum with warning
- [ ] Emulator.zig integrates all three components with stepInstruction/stepMCycle/runForFrames API
- [ ] Open-bus returns 0xFF (D-13)
- [ ] Echo RAM mirrors WRAM (BUS-02)
- [ ] Requested capacity: CORE-01 (header parse), CPU-01 (all opcodes), CPU-03 (HALT), CPU-04 (quirks), CPU-05 (M-cycle timing), BUS-01 (address dispatch), BUS-02 (echo RAM), BUS-04 (cycle accounting), CART-01 (ROM-only)
</success_criteria>

<output>
Create `.planning/phases/01-hello-rom/01-02-SUMMARY.md` when done
</output>
